import SwiftUI
//random delete later
struct VerticalSpacerCoverNavbar: View {
    var body: some View {
        Spacer()
            .frame(height: 100)
            .background(Color("primaryBackground"))
    }
}
